



# 02/17/26
- Always select best model after training
- Use regularizers
- Store models that have been trained on data already to save time
- Don't just store how many seeds you compute, but what seeds were computed. I could also add a seed offset to give the user a simple way to change the used seeds.
- Add a better storage system. Idea: each dataset, figure, model is linked to a file with all config data, date, etc. When rerunning sth, I don't always need to compare ALL parameters or deal with changes if I didn't mention some parameters. I could just write a function that retrieves the right plots for me then :) Or maybe only the results data, and then plot the results. I could then optionally save them. But this might be for later.
- Save sim performance after each sim so when I cancel & rerun, I don't lose everything?
- Store the right parameters for different settings in config
- Merge different git branches ...
- Add script with parameter sweep for finding the right parameters?
- Add time to epoch printing


# Old
- Change that Bender's Decomposition takes a graph and not a shortest path optimizer instance
- Change the requirements.txt file so that it only lists the required libraries (not a shell script), so that the libraries can be installed with pip install requirements.txt
- Figure out the imports (from src, weird first commands in notebooks, from Main.py)
- Change module setup (Bender's decomp. is a solver, rename ShortestPath.py and ShortestPathGrid.py to Graph.py and Grid.py, respectively. Possibly make a broader restructurization (e.g., add an abstract solver class).)
- Use a uniform naming convention
- Use a more uniform naming system for Notebooks, too
- Create scripts that can be run directly from Main.py
- Add a cost parameter to ShortestPathGrb's solve-function so that I can change the cost of it's optmodel and solve with that cost in one line.